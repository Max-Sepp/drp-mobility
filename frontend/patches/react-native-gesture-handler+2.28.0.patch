diff --git a/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js b/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
index 38a68f3..2c13d6b 100644
--- a/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
+++ b/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
@@ -227,7 +227,7 @@ function isRNSVGElement(viewRef) {
 function isRNSVGNode(node) {
   // If `ref` has `rngh` field, it means that component comes from Gesture Handler. This is a special case for
   // `Text` component, which is present in `RNSVGElements` set, yet we don't want to treat it as SVG.
-  if (node.ref?.rngh) {
+  if ((node.props?.ref ?? node.ref)?.rngh) {
     return false;
   }
   return Object.getPrototypeOf(node?.type)?.name === 'WebShape' || RNSVGElements.has(node?.type?.displayName);
diff --git a/node_modules/react-native-gesture-handler/lib/module/web/utils.js b/node_modules/react-native-gesture-handler/lib/module/web/utils.js
index e9ae250..789068a 100644
--- a/node_modules/react-native-gesture-handler/lib/module/web/utils.js
+++ b/node_modules/react-native-gesture-handler/lib/module/web/utils.js
@@ -215,7 +215,7 @@ export function isRNSVGElement(viewRef) {
 export function isRNSVGNode(node) {
   // If `ref` has `rngh` field, it means that component comes from Gesture Handler. This is a special case for
   // `Text` component, which is present in `RNSVGElements` set, yet we don't want to treat it as SVG.
-  if (node.ref?.rngh) {
+  if ((node.props?.ref ?? node.ref)?.rngh) {
     return false;
   }
   return Object.getPrototypeOf(node?.type)?.name === 'WebShape' || RNSVGElements.has(node?.type?.displayName);
